# 列挙体ディスパッチのexample
## ナイーブな実装
```
cargo run --bin naive
```
## enum_dispatchを用いた実装
```
cargo run --bin enum_dispatch
```
## ambassadorを用いた実装
```
cargo run --bin ambassador
```
## トレイトオブジェクトを用いた実装
```
cargo run --bin trait_object
```